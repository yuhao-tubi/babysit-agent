node_modules/
dist/
*.log
config.json
.env
.DS_Store
.run/
# local runtime state (db, repo clones) lives under ~/.babysit-agent by default
data/
# Docker deployment bind-mount: creds + config + all state (see docker-compose.yml)
.data/
